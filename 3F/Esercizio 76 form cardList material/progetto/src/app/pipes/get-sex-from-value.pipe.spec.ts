import { GetSexFromValuePipe } from './get-sex-from-value.pipe';

describe('GetSexFromValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSexFromValuePipe();
    expect(pipe).toBeTruthy();
  });
});
